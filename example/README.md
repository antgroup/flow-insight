# Flow Insight Example

This is an example application demonstrating the usage of the [@ant-ray/flow-insight](https://github.com/antgroup/flow-insight) component library for Flow visualization and insights.

## Getting Started

To run this example:

1. Install dependencies:
```bash
npm install
```

2. Start the development server:
```bash
npm run dev
```

This will open the application in your browser at http://localhost:3000.

## What's Inside

This example demonstrates:

- Basic usage of the `FlowInsight` component
- Visualizing a simple flow with nodes and edges
- Styling and layout options

## Structure

```
example/
├── public/              # Static assets
├── src/                 # Source code
│   ├── App.tsx          # Main application component
│   ├── App.css          # Application styles
│   ├── main.tsx         # Entry point
│   └── index.css        # Global styles
├── package.json         # Dependencies and scripts
└── README.md            # This file
``` 